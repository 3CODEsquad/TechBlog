@{
    ViewBag.Title = "Home";
}

@model List<TechBlog.Models.Post>

<div class="jumbotron">
    <h1>3CODEsquad</h1>
</div>

<body class="background-index">
    <aside class="side">
        <div class="aside-widget">
            <header class="recent-posts">
                <h3>Recent Posts</h3>
            </header>
            <div class="body">
                <ul class="tales-list">
                    @foreach (var recentPost in ViewBag.SidebarPosts)
            {
                        <li><a href="/Posts/Details/@recentPost.Id">@recentPost.Title</a></li>
                    }
                </ul>
            </div>
        </div>
    </aside>
    <main>
        @foreach (var post in Model)
        {
            <div class="row">
                <div class="post col-md-6">
                    <h2 class="title">@post.Title</h2>
                    <div class="about">
                        Posted on <i>@post.Date</i>
                        @if (post.Author != null)
                {
                            @:by <i>@post.Author.FullName (@post.Author.UserName)</i>
                }

                    </div>

                    <div class="body">Post content comes here</div>
                </div>
            </div>
        }
    </main>
</body>
